<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <% if(locals.errorMessage){ %>
        <div class="container">
            <p><%=locals.errorMessage%></p>
            <p>Click <a href='/'>here</a> to go back to the main page.</p>
        </div>
    <% } else{ %>
        <div class="container">
            <p>There has been an error! Click <a href='/'>here</a> to go back to the main page.</p>
        </div>
    <% } %>
</body>
</html>